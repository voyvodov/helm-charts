{{- $createPvc := and (empty .Values.storage.persistentVolumeClaimName) (.Values.storage.requestedSize) }}
{{- $deployWithNonHA := and (not .Values.haMode.enabled) (.Values.useDeploymentWhenNonHA) }}
{{- $fullname := include "redis.fullname" . }}
apiVersion: apps/v1
{{- if $deployWithNonHA }}
kind: Deployment
{{- else }}
kind: StatefulSet
{{- end }}
metadata:
  name: {{ $fullname }}
  labels:
    {{- include "redis.labels" . | nindent 4 }}
    {{- with .Values.customLabels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.customAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.haMode.enabled }}
  replicas: {{ .Values.haMode.replicas }}
  {{- else }}
  replicas: 1
  {{- end }}
  {{- if .Values.revisionHistoryLimit }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  {{- end }}
  {{- if or (.Values.haMode.enabled) (not .Values.useDeploymentWhenNonHA) }}
  serviceName: {{ $fullname }}-headless
  podManagementPolicy: {{ .Values.podManagementPolicy }}
  updateStrategy:
    type: {{ .Values.updateStrategyType }}
  {{- if or .Values.storage.persistentVolumeClaimRetentionPolicy.whenDeleted .Values.storage.persistentVolumeClaimRetentionPolicy.whenScaled }}
  persistentVolumeClaimRetentionPolicy:
    {{- with .Values.storage.persistentVolumeClaimRetentionPolicy.whenDeleted }}
    whenDeleted: {{ . }}
    {{- end }}
    {{- with .Values.storage.persistentVolumeClaimRetentionPolicy.whenScaled }}
    whenScaled: {{ . }}
    {{- end }}
  {{- end }}
  {{- else }}
  strategy:
    type: Recreate
  {{- end }}
  selector:
    matchLabels:
      {{- include "redis.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/customconfig: {{ include (print $.Template.BasePath "/config.yml") . | sha256sum }}
        checksum/scripts: {{ include (print $.Template.BasePath "/scripts.yml") . | sha256sum }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "redis.selectorLabels" . | nindent 8 }}
      {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- if .Values.priorityClassName }}
      priorityClassName: {{ .Values.priorityClassName }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "redis.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: {{ .Chart.Name }}-init
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
          {{- if .Values.redis.authentication.enabled }}
            - name: REDISCLI_AUTH
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.redis.authentication.existingSecret }}
                  key: redis-password
          {{- end }}
          {{- with .Values.env }}
            {{- toYaml . | nindent 12 }}
          {{- end }}          
          envFrom:
          {{- range .Values.extraInitEnvSecrets }}
            - secretRef:
                name: {{ . }}
          {{- end }}

          volumeMounts:
            - mountPath: /data
              name: {{ .Values.storage.volumeName }}
            - name: scripts
              mountPath: /scripts
            - mountPath: /usr/local/etc/redis/redis.conf
              subPath: redis.conf
              name: config
            - mountPath: /usr/local/etc/redis/sentinel.conf
              subPath: sentinel.conf
              name: config
          {{- if .Values.redis.extraSecretConfigs }}
            - mountPath: /extraredisconfigs
              name: extraredisconfigs-volume
          {{- end }}
          {{- if .Values.sentinel.extraSecretConfigs }}
            - mountPath: /extrasentinelconfigs
              name: extrasentinelconfigs-volume
          {{- end }}
          {{- with .Values.initResources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          command: [ "/scripts/init.sh" ]
      {{- with .Values.extraInitContainers }}
      {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}-server
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: redis
              containerPort: 6379
              protocol: TCP
          {{- if .Values.customStartupProbe }}
          startupProbe:
            {{- toYaml .Values.customStartupProbe | nindent 12 }}
          {{- else }}
          {{- if .Values.startupProbe.enabled }}
          startupProbe:
            exec:
              command:
                - sh
                - -c
                {{ if .Values.tls.enabled }}
                - "redis-cli --tls --cacert /tls/ca.crt ping"
                {{ else }}
                - "redis-cli ping"
                {{ end }}
          {{- with .Values.startupProbe }}
            initialDelaySeconds: {{ .initialDelaySeconds }}
            timeoutSeconds: {{ .timeoutSeconds }}
            failureThreshold: {{ .failureThreshold }}
            successThreshold: {{ .successThreshold }}
            periodSeconds: {{ .periodSeconds }}
          {{- end }}
          {{- end }}
          {{- end }}
          {{- if .Values.customLivenessProbe }}
          livenessProbe:
            {{- toYaml .Values.customLivenessProbe | nindent 12 }}
          {{- else }}
          {{- if .Values.livenessProbe.enabled }}              
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                {{ if .Values.tls.enabled }}
                - "redis-cli --tls --cacert /tls/ca.crt ping"
                {{ else }}
                - "redis-cli ping"
                {{ end }}
          {{- with .Values.livenessProbe }}
            initialDelaySeconds: {{ .initialDelaySeconds }}
            timeoutSeconds: {{ .timeoutSeconds }}
            failureThreshold: {{ .failureThreshold }}
            successThreshold: {{ .successThreshold }}
            periodSeconds: {{ .periodSeconds }}                
          {{- end }}
          {{- end }}
          {{- end }}
          {{- if .Values.customReadinessProbe }}
          readinessProbe:
            {{- toYaml .Values.customReadinessProbe | nindent 12 }}
          {{- else }}
          {{- if .Values.readinessProbe.enabled }}
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                {{ if .Values.tls.enabled }}
                - "redis-cli --tls --cacert /tls/ca.crt ping"
                {{ else }}
                - "redis-cli ping"
                {{ end }}
          {{- with .Values.readinessProbe }}
            initialDelaySeconds: {{ .initialDelaySeconds }}
            timeoutSeconds: {{ .timeoutSeconds }}
            failureThreshold: {{ .failureThreshold }}
            successThreshold: {{ .successThreshold }}
            periodSeconds: {{ .periodSeconds }}                
          {{- end }}
          {{- end }}
          {{- end }}
          {{- with .Values.redis.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          command:
            - redis-server
          args:
            - /data/conf/redis.conf
            {{- range .Values.redis.args }}
            - {{ . }}
            {{- end }}
          env:
            {{- if .Values.redis.authentication.enabled }}
            - name: REDISCLI_AUTH
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.redis.authentication.existingSecret }}
                  key: redis-password
            {{- end }}
            {{- with .Values.env }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          envFrom:
          {{- range .Values.redis.extraEnvSecrets }}
            - secretRef:
                name: {{ . }}
          {{- end }}
          volumeMounts:
            - name: {{ .Values.storage.volumeName }}
              mountPath: /data
            {{- if .Values.tls.enabled }}
            - name: tls-secret
              mountPath: /tls
            {{- end }}
            {{- range $secret := .Values.redis.extraSecrets }}
            - name: {{ $secret.name }}-redis
              mountPath: {{ $secret.mountPath }}
            {{- end }}
            {{- range $config := .Values.redis.extraConfigs }}
            - name: {{ $config.name }}-redis
              mountPath: {{ $config.mountPath }}
            {{- end }}
            {{- range $storage := .Values.extraStorage }}
            - name: {{ $storage.name }}
              mountPath: {{ $storage.mountPath }}
            {{- end }}
        {{- if .Values.haMode.enabled }}
        - name: {{ .Chart.Name }}-sentinel
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: sentinel
              containerPort: 26379
              protocol: TCP
          {{- if .Values.customLivenessProbe }}
          livenessProbe:
            {{- toYaml .Values.customLivenessProbe | nindent 12 }}
          {{- else }}
          {{- if .Values.livenessProbe.enabled }}              
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                {{- if .Values.tls.enabled }}
                - "redis-cli --tls --cacert /tls/ca.crt -p 26379 ping"
                {{- else }}
                - redis-cli -p 26379 ping
                {{- end }}
          {{- with .Values.livenessProbe }}
            initialDelaySeconds: {{ .initialDelaySeconds }}
            timeoutSeconds: {{ .timeoutSeconds }}
            failureThreshold: {{ .failureThreshold }}
            successThreshold: {{ .successThreshold }}
            periodSeconds: {{ .periodSeconds }}                
          {{- end }}
          {{- end }}
          {{- end }}
          {{- if .Values.customReadinessProbe }}
          readinessProbe:
            {{- toYaml .Values.customReadinessProbe | nindent 12 }}
          {{- else }}
          {{- if .Values.readinessProbe.enabled }}
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                {{- if .Values.tls.enabled }}
                - "redis-cli --tls --cacert /tls/ca.crt -p 26379 ping"
                {{- else }}
                - redis-cli -p 26379 ping
                {{- end }}
          {{- with .Values.readinessProbe }}
            initialDelaySeconds: {{ .initialDelaySeconds }}
            timeoutSeconds: {{ .timeoutSeconds }}
            failureThreshold: {{ .failureThreshold }}
            successThreshold: {{ .successThreshold }}
            periodSeconds: {{ .periodSeconds }}                
          {{- end }}
          {{- end }}
          {{- end }}
          {{- with .Values.sentinelResources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          command:
            - redis-sentinel
          args:
            - /data/conf/sentinel.conf
            {{- range .Values.sentinel.args }}
            - {{ . }}
            {{- end }}
          {{- with .Values.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          envFrom:
          {{- range .Values.sentinel.extraEnvSecrets }}
            - secretRef:
                name: {{ . }}
          {{- end }}
          volumeMounts:
            - name: {{ .Values.storage.volumeName }}
              mountPath: /data
            {{- if .Values.tls.enabled }}
            - name: tls-secret
              mountPath: /tls
            {{- end }}
            {{- range $secret := .Values.sentinel.extraSecrets }}
            - name: {{ $secret.name }}-sentinel
              mountPath: {{ $secret.mountPath }}
            {{- end }}
            {{- range $config := .Values.sentinel.extraConfigs }}
            - name: {{ $config.name }}-sentinel
              mountPath: {{ $config.mountPath }}
            {{- end }}
        {{- end }}
        {{- if .Values.metrics.enabled }}
        {{- with .Values.metrics }}
        - name: {{ $.Chart.Name }}-exporter
          {{- with .exporter.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .exporter.image.registry }}/{{ .exporter.image.repository }}:{{ .exporter.image.tag }}"
          imagePullPolicy: {{ .exporter.image.pullPolicy }}
          ports:
            - name: exporter
              containerPort: {{ .service.containerPort }}
              protocol: TCP
          {{- with .exporter.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .exporter.args }}
          args:
            {{- range . }}
            - {{ . }}
            {{- end }}
          {{- end }}  
          env:
            {{- if $.Values.tls.enabled }}
            - name: REDIS_ADDR
              value: "rediss://localhost:6379"
            - name: REDIS_EXPORTER_SKIP_TLS_VERIFICATION
              value: "true"
            {{- end }}
            {{- if $.Values.redis.authentication.enabled }}
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.redis.authentication.existingSecret }}
                  key: redis-password
            {{- end }}
          {{- with .exporter.env }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          envFrom:
          {{- range .exporter.extraExporterEnvSecrets }}
            - secretRef:
                name: {{ . }}
          {{- end }}
          {{- if .exporter.customStartupProbe }}
          startupProbe:
            {{- toYaml .exporter.customStartupProbe | nindent 12 }}
          {{- else }}
          {{- if .exporter.startupProbe.enabled }}
          startupProbe:
            httpGet:
              path: /
              port: exporter
          {{- with .exporter.startupProbe }}
            initialDelaySeconds: {{ .initialDelaySeconds }}
            timeoutSeconds: {{ .timeoutSeconds }}
            failureThreshold: {{ .failureThreshold }}
            successThreshold: {{ .successThreshold }}
            periodSeconds: {{ .periodSeconds }}
          {{- end }}
          {{- end }}
          {{- end }}
          {{- if .exporter.customLivenessProbe }}
          livenessProbe:
            {{- toYaml .exporter.customLivenessProbe | nindent 12 }}
          {{- else }}
          {{- if .exporter.livenessProbe.enabled }}              
          livenessProbe:
            httpGet:
              path: /
              port: exporter
          {{- with .exporter.livenessProbe }}
            initialDelaySeconds: {{ .initialDelaySeconds }}
            timeoutSeconds: {{ .timeoutSeconds }}
            failureThreshold: {{ .failureThreshold }}
            successThreshold: {{ .successThreshold }}
            periodSeconds: {{ .periodSeconds }}                
          {{- end }}
          {{- end }}
          {{- end }}
          {{- if .exporter.customReadinessProbe }}
          readinessProbe:
            {{- toYaml .exporter.customReadinessProbe | nindent 12 }}
          {{- else }}
          {{- if .exporter.readinessProbe.enabled }}
          readinessProbe:
            httpGet:
              path: /
              port: exporter
          {{- with .exporter.readinessProbe }}
            initialDelaySeconds: {{ .initialDelaySeconds }}
            timeoutSeconds: {{ .timeoutSeconds }}
            failureThreshold: {{ .failureThreshold }}
            successThreshold: {{ .successThreshold }}
            periodSeconds: {{ .periodSeconds }}                
          {{- end }}
          {{- end }}
          {{- end }}
          volumeMounts:
            {{- range $secret := .exporter.extraExporterSecrets }}
            - name: {{ $secret.name }}-exporter
              mountPath: {{ $secret.mountPath }}
            {{- end }}
            {{- range $config := .exporter.extraExporterConfigs }}
            - name: {{ $config.name }}-exporter
              mountPath: {{ $config.mountPath }}
            {{- end }}
        {{- end }}
        {{- end }}
      {{- with .Values.extraContainers }}
      {{- toYaml . | nindent 8 }}
      {{- end }} 
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: scripts
          configMap:
            name: {{ $fullname }}-scripts
            defaultMode: 0555
        - name: config
          configMap:
            name: {{ $fullname }}
        {{- if .Values.tls.enabled }}
        - name: tls-secret
          secret:
          {{- if .Values.tls.existingSecret }}
            secretName: {{ .Values.tls.existingSecret }}
          {{- else }}
            secretName: {{ $fullname }}-tls
          {{- end }}
            defaultMode: 0440
        {{- end }}
        {{- range $secret := .Values.redis.extraSecrets }}
        - name: {{ $secret.name }}-redis
          secret:
            secretName: {{ $secret.name }}
            defaultMode: {{ $secret.defaultMode | default 0440 }}
        {{- end }}
        {{- range $config := .Values.redis.extraConfigs }}
        - name: {{ $config.name }}-redis
          configMap:
            name: {{ $config.name }}
            defaultMode: {{ $config.defaultMode | default 0440 }}
        {{- end }}
        {{- range $secret := .Values.sentinel.extraSecrets }}
        - name: {{ $secret.name }}-sentinel
          secret:
            secretName: {{ $secret.name }}
            defaultMode: {{ $secret.defaultMode | default 0440 }}
        {{- end }}
        {{- range $config := .Values.sentinel.extraConfigs }}
        - name: {{ $config.name }}-sentinel
          configMap:
            name: {{ $config.name }}
            defaultMode: {{ $config.defaultMode | default 0440 }}
        {{- end }}
        {{- if .Values.metrics.enabled }}
        {{- range $secret := .Values.metrics.exporter.extraExporterSecrets }}
        - name: {{ $secret.name }}-exporter
          secret:
            secretName: {{ $secret.name }}
            defaultMode: {{ $secret.defaultMode | default 0440 }}
        {{- end }}
        {{- range $config := .Values.metrics.exporter.extraExporterConfigs }}
        - name: {{ $config.name }}-exporter
          configMap:
            name: {{ $config.name }}
            defaultMode: {{ $config.defaultMode | default 0440 }}
        {{- end }}
        {{- end }}
        {{- if .Values.redis.extraSecretConfigs }}
        - name: extraredisconfigs-volume
          secret:
            secretName: {{ .Values.redis.extraSecretConfigs }}
        {{- end }}
        {{- if .Values.sentinel.extraSecretConfigs }}
        - name: extrasentinelconfigs-volume
          secret:
            secretName: {{ .Values.sentinel.extraSecretConfigs }}
        {{- end }}
        {{- range $storage := .Values.extraStorage }}
        - name: {{ $storage.name }}
          persistentVolumeClaim:
            claimName: {{ $storage.pvcName }}
        {{- end }}
{{- with .Values.storage }}
  {{- if not $createPvc }}
        - name: {{ .volumeName }}
        {{- if .persistentVolumeClaimName }}
          persistentVolumeClaim:
            claimName: {{ .persistentVolumeClaimName }}
        {{- else }}
          emptyDir: {}
        {{- end }}
  {{- else }}
  {{- if $deployWithNonHA }}
        - name: {{ .volumeName }}
          persistentVolumeClaim:
            claimName: {{ $fullname }}
  {{- else }}
  volumeClaimTemplates:
    - metadata:
        name: {{ .volumeName }}
      {{- with .labels }}
        labels:
        {{- toYaml . | nindent 10 }}
      {{- end }}
      {{- with .annotations }}
        annotations:
        {{- toYaml . | nindent 10 }}
      {{- end }}
      spec:
        {{- with .accessModes }}
        accessModes:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- if .className }}
        storageClassName: {{ .className }}
        {{- end }}
        resources:
          requests:
            storage: {{ .requestedSize }}
  {{- end }}
  {{- end }}
{{- end }}